name: Sync Upstream FreeBSD and Build Staging

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync-and-stage:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: Configure Git Identity & Large Repo Optimizations
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Aggressive tuning kept for safety during repack/merges
          git config --global http.postBuffer 1073741824
          git config --global http.version HTTP/1.1
          git config --global core.compression 0
          git config --global pack.threads 1
          git config --global pack.windowMemory 2g

      - name: Add FreeBSD Upstream Remote & Shallow Fetch
        run: |
          git remote add upstream https://github.com/freebsd/freebsd-src.git
          
          # THE FIX: --depth=1 grabs ONLY the current tip of the branches. 
          # This creates a lightweight snapshot from "right now" forward.
          git fetch upstream --depth=1 --prune --no-tags

      - name: Dynamically Sync Upstream, Overlay Midstream, and Build Downstream
        run: |
          # Loop through upstream main and all stable/* branches, ignoring HEAD
          for ref in $(git branch -r | grep -E 'upstream/(main$|stable/)' | grep -v 'HEAD'); do
            branch_path=${ref#upstream/}                  # e.g., "main" or "stable/14"
            safe_name=$(echo "$branch_path" | tr '/' '-')   # e.g., "main" or "stable-14"

            echo "=================================================="
            echo "Processing upstream branch: $branch_path"
            echo "=================================================="

            # 1. Update the upstream mirror branch on origin with the shallow snapshot
            git push origin refs/remotes/upstream/$branch_path:refs/heads/upstream/$safe_name --force-with-lease

            # 2. Setup the downstream staging branch
            git checkout -B downstream/staging-$safe_name refs/remotes/upstream/$branch_path

            # 3. Check and merge midstream shims if present
            MIDSTREAM_BRANCH="midstream/$safe_name"
            
            if git ls-remote --exit-code --heads origin "$MIDSTREAM_BRANCH"; then
              echo "Midstream branch '$MIDSTREAM_BRANCH' found. Overlaying changes..."
              git fetch origin "$MIDSTREAM_BRANCH"
              
              # --allow-unrelated-histories added because midstream might have 
              # older commit history than the new shallow upstream tip.
              git merge --no-edit origin/"$MIDSTREAM_BRANCH" --allow-unrelated-histories || {
                echo "Error: Conflict occurred while merging $MIDSTREAM_BRANCH."
                exit 1
              }
            else
              echo "Midstream branch '$MIDSTREAM_BRANCH' not found. Proceeding with vanilla upstream."
            fi

            # 4. Push the final combined staging candidate to downstream
            git push origin downstream/staging-$safe_name --force-with-lease
          done